---
"@gribble/skills": patch
---

The skill file's `metadata.version` now follows the package version automatically instead of a hardcoded string, so `gribble init --update-skills` sees new releases.
